const Listing = require('../../../db/models/Listing');
const router = require('express').Router();

module.exports = router;

//  @desc        Create listing
//  @route       POST /admin/
//  @access      Private
